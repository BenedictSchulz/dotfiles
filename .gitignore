# macOS
.DS_Store

# Credentials & local env - NEVER commit these
.git-credentials
.ssh/
.env.local

# Neovim plugin artifacts (installed by lazy.nvim)
nvim/lazy-lock.json
nvim/lua/config/plugins/tmp/

# Node modules
node_modules/
